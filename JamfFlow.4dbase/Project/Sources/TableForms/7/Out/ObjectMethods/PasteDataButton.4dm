


  //$vt:=Request("Enter values for endpoints, one per line.";"";"Add Records";"Cancel")
  //If (OK=1)
  //SET DATABASE PARAMETER([Endpoints];Table sequence number;0)
  //$vt:=Replace string($vt;Char(Carriage return);"")
  //$ct:=Split string($vt;" ";sk ignore empty strings+sk trim spaces)
  //For ($i;1;$ct.length)
  //CREATE RECORD([Endpoints])
  //[Endpoints]API_Endpoint_Name:=$ct[$i-1]
  //SAVE RECORD([Endpoints])
  //End for 
  //End if 
